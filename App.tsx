import {Props} from 'react-native-floating-label-input/src/index.js';
import Navigator from './Navigator';
import {Component} from 'react';

export default class App extends Component<Props> {
  render() {
    return <Navigator />;
  }
}
